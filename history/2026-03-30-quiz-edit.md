# BRACE Milestone

Milestone: `Quiz.Edit`
Date: 2026-03-30
Status: in_progress
Primary mission: Continue the Playwright-BDD to WTR migration until the retained Playwright feature inventory has full WTR parity while preserving the legacy Playwright suite until parity is proven.
PLANS ref: `PLANS.md`
Commit(s): none
Related pull(s): none
Depends on: `Quiz.CreateNew`, `2026-03-30-full-wtr-parity-mission-selection`

## BRACE Plan Snapshot

### B — Behavior
- Add dedicated mocked and backend WTR coverage for `Quiz.Edit.feature`.
- Prove editing an existing quiz title and description from the workspace updates the workspace list and the downstream quiz welcome page.
- Keep the slice bounded to edit-route prepopulation, submission, and post-save visibility; do not widen into quiz validations, question filtering, clipboard, stats, or unrelated workspace behaviors.

### R — Risks (top 1–5)
1. High / Slice-level behavior integrity: the edit flow depends on fetch, prepopulation, save, workspace navigation, and downstream quiz display, so shallow assertions could miss a real contract gap.
2. Medium / Scope / sequencing drift: quiz authoring has adjacent filter and validation behaviors that could easily bleed into this slice.
3. Medium / Harness / gate reliability: the full migration gate still inherits the host-aware backend wrapper baseline and low-grade legacy Playwright residue.
4. Medium / Environment / external variability: backend proof depends on the real quiz fetch and update API plus workspace refresh behavior.

### A — Assurances
- Reuse the existing `Quiz.CreateNew` seam, workspace quiz list entry point, and quiz backend helpers instead of widening production seams.
- Assert both the edited workspace list item and the edited quiz welcome-page description so the proof covers the whole edit round-trip.
- Keep targeted mocked and backend proof separate from the full gate and only claim closure if all three levels agree.
- Treat any contradiction between targeted runs and the full gate as a real signal, not as noise to ignore.

### Planned Coverage
- Covered:
  - navigating from the workspace list into the quiz edit route
  - editing quiz title and description
  - saving the edited quiz and returning to the workspace
  - taking the edited quiz and seeing the updated description
  - mocked and backend lanes in Chromium and Firefox
- Not covered:
  - quiz form validations
  - question filtering and randomized-count logic
  - clipboard, stats, or broader workspace list actions
  - Cheapest proof: implement focused mocked and backend WTR tests for `Quiz.Edit`, then run `bash ./scripts/test-migration.sh`

### Planned Evidence
- Intent: prove the edit flow under deterministic mocked responses
  Command / artifact: `WTR_CONCURRENT_BROWSERS=1 WTR_CONCURRENCY=1 pnpm --dir frontend exec web-test-runner --config web-test-runner.config.mjs --files "tests/wtr/mocked/quiz-edit.test.tsx"`
  Result: pending
  Interpretation: will prove route-local edit behavior without backend variability
- Intent: prove the same edit flow against the real backend
  Command / artifact: `pnpm --dir specs exec start-server-and-test "concurrently \"cd /workspaces/quizmaster/backend && ./gradlew bootRun\" \"cd /workspaces/quizmaster/frontend && pnpm dev\"" "8080|5173" "WTR_API_PROXY_TARGET=http://localhost:8080 WTR_VITE_HOST=127.0.0.1 WTR_CONCURRENT_BROWSERS=1 WTR_CONCURRENCY=1 WTR_TEST_TIMEOUT=20000 pnpm --dir /workspaces/quizmaster/frontend exec web-test-runner --config /workspaces/quizmaster/frontend/web-test-runner.config.mjs --files \"tests/wtr/backend/quiz-edit.backend.test.tsx\""`
  Result: pending
  Interpretation: will prove the same contract against the real quiz edit API and workspace refresh path
- Intent: prove the slice against the acceptance floor
  Command / artifact: `bash ./scripts/test-migration.sh`
  Result: pending
  Interpretation: will prove the new slice does not break the accepted migration baseline

### Planned Scope Inventory
- `frontend/tests/wtr/mocked/quiz-edit.test.tsx`
- `frontend/tests/wtr/backend/quiz-edit.backend.test.tsx`
- `frontend/tests/wtr/support/backend-api.ts`
- `PLANS.md`
- `history/2026-03-30-quiz-edit.md`

### Exit condition
- Both targeted `Quiz.Edit` files are green in Chromium and Firefox, the full migration gate is green or explicitly marked `UNPROVEN`, and `PLANS.md` points to the correct next action at closeout.

### Planned Trace
| Behavior | Risk (Tier / Area) | Assurance | Planned Evidence | Residual risk | Cheapest proof |
|---|---|---|---|---|---|
| Editing quiz title and description updates the workspace list | High / Slice-level behavior integrity | Drive the edit flow from the real workspace entry point and assert the post-save workspace list item | Targeted mocked and backend edit runs | Medium until both targeted lanes pass | Re-run the edit tests after future quiz-form changes |
| Taking the edited quiz shows the updated description | High / Slice-level behavior integrity | Extend the proof through the downstream welcome page rather than stopping at the save redirect | Targeted mocked and backend edit runs | Medium until both targeted lanes pass | Re-run the edit tests after future quiz-display changes |
| Full migration confidence remains intact | Medium / Harness / gate reliability | Run the command of record after targeted proof | `bash ./scripts/test-migration.sh` | Low-Medium | Apply the contradiction protocol only if the full gate conflicts with targeted proof |

## Execution Notes
- This milestone was instantiated from the repo-wide mission-selection artifact.
- The mission is now explicitly active and the prior planning artifacts have been packaged into a clean bookkeeping boundary before implementation starts.

## Pulls Handled During This Milestone
- None yet.

## Current State
- What is true right now: `Quiz.Edit.feature` has no dedicated WTR coverage yet, while the quiz edit route, workspace edit button, and backend quiz helpers already exist in the repo.
- What remains blocked / incomplete: the mocked and backend WTR tests still need to be written and executed, and the full migration gate has not yet been rerun with quiz-edit coverage in place.
- Current evidence or hydration notes: repo inspection shows `quiz-edit-page.tsx` and the workspace quiz-item edit button already wire the feature seam; current WTR coverage already proves `Quiz.CreateNew`, so this slice can reuse the same quiz-authoring foundation. The mission-level risk areas carry over substantively from the prior mission, so no new default mission risk class was introduced at activation time.
- Next action / cheapest proof: implement `tests/wtr/mocked/quiz-edit.test.tsx` and `tests/wtr/backend/quiz-edit.backend.test.tsx`, then run the targeted commands above.
