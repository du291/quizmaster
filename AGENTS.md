# Quizmaster Agent Notes

- For testing changes around infrastructure or external-world seams (time, files, API/network, randomness, persistence, browser APIs), use the `infrastructure-seams` skill and the repo guide at `docs/testing/infrastructure-seams.md`.
